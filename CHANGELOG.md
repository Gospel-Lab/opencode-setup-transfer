# 변경 기록 / Changelog

이 파일은 사용자가 알아야 할 변화만 적습니다. 최신이 위입니다.

## [0.3.0] — 2026-08-15

### 추가
- **opencode·Claude Code·codex 세 도구 지원.** 실행하는 컴퓨터에 깔려 있는 것을 그때그때 찾아
  각각의 설정을 담습니다. 없는 도구는 건너뛰고, 하나도 없으면 이유를 알려주고 멈춥니다.
  설정 폴더는 `CLAUDE_CONFIG_DIR`·`CODEX_HOME` 환경변수와 `opencode debug paths` 로 확인합니다.
- 아카이브가 도구별 폴더로 나뉩니다. 이전 형식도 그대로 읽습니다.
- Claude Code 플러그인 목록과 opencode 플러그인 목록을 각각 재설치 명령으로 남깁니다.

### 고침
- **도구가 설치됐지만 한 번도 실행되지 않은 컴퓨터에서 내보내기가 조용히 죽던 문제.**
  설정 폴더를 찾지 못했을 때 함수가 실패로 끝나 스크립트 전체가 종료됐습니다.
  "없음"은 오류가 아니므로 정상 결과로 처리합니다.
- 파일 경로를 비밀값으로 오인해 자리표시자로 지우던 문제.

## [0.2.0] — 2026-08-15

### 추가
- **연결 마법사 `connect.sh`** — GitHub·Firebase·Vercel·Supabase·Netlify 다섯 서비스를 하나씩 안내.
  CLI 설치, 가입 페이지 열기, 로그인 화면 안내, 연결 검증까지 이어집니다.
  `connect.sh check` 로 상태만 볼 수도 있습니다.
- **플러그인 자동 복원** — `package.json` 의 의존성을 병합합니다.
  opencode 가 다음 실행 때 스스로 설치하므로 플러그인이 되살아납니다.
- **영어 문서** — [README.en.md](README.en.md)
- **[SECURITY.md](SECURITY.md)** — 자격증명을 어떻게 다루는지, 무엇을 담지 않는지 명시.

### 변경
- **가져오기가 기본으로 자동 적용됩니다.** 항목별로 확인하려면 `import.sh --ask`.
- **결과 파일 저장 위치를 환경에 맞게 판별합니다.** WSL 에서는 윈도우 쪽 바탕화면에 저장해,
  탐색기에서 바로 찾을 수 있습니다. (이전에는 리눅스 안쪽 `~/Desktop` 에 저장돼 보이지 않았습니다)
- 공개 배포에 맞게 문구를 정리했습니다.

### 고침
- **설정 폴더를 잘못 읽던 문제** — `OPENCODE_CONFIG_DIR` 을 덮어쓰는 실행 환경에서
  엉뚱한 폴더를 내보내며 빈 아카이브가 만들어졌습니다. 이제 `opencode debug paths` 로 확인합니다.
- **새 컴퓨터에서 멈추던 문제** — 로그인되지 않은 CLI 호출이 응답을 기다리며 멈췄습니다.
  모든 외부 호출에 제한 시간을 걸었습니다.
- **계정명에 색상 코드가 섞이던 문제** — `39mme@example.com` 처럼 표시되던 것을 고쳤습니다.
- **macOS 기본 bash(3.2)에서 실행되지 않던 문제** — 변수 선언과 문자열 처리 방식을 맞췄습니다.

## [0.1.0] — 2026-08-15

첫 공개.

- `export.sh` — 화이트리스트 수집, 심볼릭 링크 실체화, 비밀값 자리표시자 치환,
  홈 경로 템플릿화, 비밀정보·본인 식별정보 2단 스캔
- `import.sh` — 병합 방식 적용, 가져오기 전 백업, `opencode debug config` 로 작동 검증
- `connections.sh` — 연결된 서비스 기록과 재로그인 안내
- `install.sh` — 한 줄 설치
